import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'expense-center-user-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
