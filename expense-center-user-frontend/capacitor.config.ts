import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.expense.center',
  appName: 'Expense Center',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
